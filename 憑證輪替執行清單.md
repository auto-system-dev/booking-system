# 憑證輪替執行清單

## 目標

立即停用舊憑證，改用新憑證，並確認系統功能正常。

## 目前已完成（本機）

- [x] 程式已禁止使用硬編碼密碼/金鑰 fallback。
- [x] 已建立新 `SESSION_SECRET` 並寫入本機 `.env`。

## 需在外部平台手動完成（高優先）

### 1) Gmail / 郵件憑證

- [ ] 重新產生 Gmail App Password（或改用 OAuth2 憑證）。
- [ ] 在 Railway 變數更新：
  - `EMAIL_USER`
  - `EMAIL_PASS`（若不用 OAuth2）
  - `GMAIL_CLIENT_ID` / `GMAIL_CLIENT_SECRET` / `GMAIL_REFRESH_TOKEN`（若用 OAuth2）
- [ ] 驗證：後台測試郵件可成功送達。

### 2) 綠界支付憑證

- [ ] 到綠界後台確認是否可輪替 HashKey / HashIV（正式環境優先）。
- [ ] 在 Railway 或後台系統設定更新：
  - 測試環境：`ECPAY_MERCHANT_ID` / `ECPAY_HASH_KEY` / `ECPAY_HASH_IV`
  - 正式環境：`ECPAY_MERCHANT_ID_PROD` / `ECPAY_HASH_KEY_PROD` / `ECPAY_HASH_IV_PROD`
- [ ] 驗證：建立支付表單成功，回傳驗證成功（CheckMacValue 正常）。

### 3) Cloudflare R2 憑證

- [ ] 在 Cloudflare 建立新的 R2 Access Key。
- [ ] 更新 Railway 變數：
  - `R2_ACCESS_KEY_ID`
  - `R2_SECRET_ACCESS_KEY`
  - `R2_ACCOUNT_ID`
  - `R2_BUCKET_NAME`
  - `R2_PUBLIC_URL`
- [ ] 刪除舊 Access Key。
- [ ] 驗證：圖片上傳與讀取正常。

### 4) Session 憑證同步（所有環境）

- [ ] 在 Railway 設定新的 `SESSION_SECRET`（建議與本機不同）。
- [ ] 重新部署後驗證：可正常登入、Cookie 正常建立。

## 驗收標準

- [ ] `SESSION_SECRET`、郵件、綠界、R2 皆使用新值。
- [ ] 舊憑證已停用（刪除或失效）。
- [ ] 功能驗證通過：登入、寄信、訂房刷卡、圖片上傳。

## 風險提醒

- 輪替完成後，現有登入 Session 可能失效，屬正常現象。
- 若先刪除舊憑證但尚未更新新值，系統會啟動失敗或功能中斷。
