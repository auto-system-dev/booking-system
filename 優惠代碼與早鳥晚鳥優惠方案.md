# 優惠代碼與早鳥/晚鳥優惠方案

## 📋 功能概述

### 1. 優惠代碼功能
- ✅ 固定金額折抵（例如：NT$ 500）
- ✅ 百分比折抵（例如：10%）
- ✅ 使用次數限制（總使用次數、每人使用次數）
- ✅ 有效期設定（開始日期、結束日期）
- ✅ 最低消費金額限制
- ✅ 適用房型限制（可選）
- ✅ 與其他優惠的組合規則

### 2. 早鳥/晚鳥優惠
- ✅ 早鳥優惠：提前 N 天預訂可享折扣
- ✅ 晚鳥優惠：入住前 N 天內預訂可享折扣
- ✅ 可設定折扣類型（固定金額或百分比）
- ✅ 可設定適用日期範圍
- ✅ 可與優惠代碼疊加或互斥

---

## 🗄️ 資料庫設計

### 優惠代碼表 (promo_codes)

```sql
CREATE TABLE promo_codes (
    id SERIAL PRIMARY KEY,
    code VARCHAR(50) UNIQUE NOT NULL,                    -- 優惠代碼（例如：SUMMER2024）
    name VARCHAR(255) NOT NULL,                           -- 優惠名稱（例如：夏日特惠）
    description TEXT,                                    -- 優惠說明
    discount_type VARCHAR(20) NOT NULL,                   -- 折扣類型：fixed（固定金額）或 percent（百分比）
    discount_value DECIMAL(10,2) NOT NULL,              -- 折扣值（金額或百分比）
    min_spend INTEGER DEFAULT 0,                         -- 最低消費金額（0 表示無限制）
    max_discount INTEGER DEFAULT NULL,                    -- 最高折抵金額（百分比折扣時使用，NULL 表示無限制）
    applicable_room_types TEXT,                          -- 適用房型（JSON 陣列，NULL 表示全部適用）
    total_usage_limit INTEGER DEFAULT NULL,              -- 總使用次數限制（NULL 表示無限制）
    per_user_limit INTEGER DEFAULT 1,                    -- 每人使用次數限制
    start_date DATE,                                      -- 開始日期（NULL 表示立即生效）
    end_date DATE,                                        -- 結束日期（NULL 表示永久有效）
    is_active INTEGER DEFAULT 1,                          -- 是否啟用（1=啟用，0=停用）
    can_combine_with_early_bird INTEGER DEFAULT 0,      -- 可與早鳥優惠疊加（1=是，0=否）
    can_combine_with_late_bird INTEGER DEFAULT 0,        -- 可與晚鳥優惠疊加（1=是，0=否）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 優惠代碼使用記錄表 (promo_code_usages)

```sql
CREATE TABLE promo_code_usages (
    id SERIAL PRIMARY KEY,
    promo_code_id INTEGER NOT NULL,                      -- 優惠代碼 ID
    booking_id VARCHAR(255) NOT NULL,                    -- 訂房編號
    guest_email VARCHAR(255) NOT NULL,                   -- 客戶 Email
    discount_amount DECIMAL(10,2) NOT NULL,             -- 實際折抵金額
    original_amount DECIMAL(10,2) NOT NULL,             -- 原始金額
    final_amount DECIMAL(10,2) NOT NULL,                 -- 折抵後金額
    used_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (promo_code_id) REFERENCES promo_codes(id) ON DELETE CASCADE
);
```

### 早鳥/晚鳥優惠設定表 (early_late_bird_settings)

```sql
CREATE TABLE early_late_bird_settings (
    id SERIAL PRIMARY KEY,
    type VARCHAR(20) NOT NULL,                           -- 類型：early_bird（早鳥）或 late_bird（晚鳥）
    name VARCHAR(255) NOT NULL,                          -- 優惠名稱（例如：早鳥優惠 30 天）
    description TEXT,                                    -- 優惠說明
    days_before_checkin INTEGER NOT NULL,                -- 入住前 N 天（早鳥）或入住前 N 天內（晚鳥）
    discount_type VARCHAR(20) NOT NULL,                   -- 折扣類型：fixed 或 percent
    discount_value DECIMAL(10,2) NOT NULL,               -- 折扣值
    max_discount INTEGER DEFAULT NULL,                    -- 最高折抵金額（百分比折扣時使用）
    min_nights INTEGER DEFAULT 1,                        -- 最低住宿天數（0 表示無限制）
    applicable_room_types TEXT,                          -- 適用房型（JSON 陣列，NULL 表示全部適用）
    start_date DATE,                                      -- 開始日期
    end_date DATE,                                        -- 結束日期
    is_active INTEGER DEFAULT 1,                         -- 是否啟用
    priority INTEGER DEFAULT 0,                          -- 優先級（數字越大越優先，用於多個優惠時選擇）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🔧 後端 API 設計

### 優惠代碼 API

#### 1. 驗證優惠代碼
```
POST /api/promo-codes/validate
Body: {
    code: "SUMMER2024",
    totalAmount: 5000,
    roomType: "標準雙人房",
    checkInDate: "2024-06-01"
}

Response: {
    success: true,
    data: {
        code: "SUMMER2024",
        name: "夏日特惠",
        discount_type: "percent",
        discount_value: 10,
        discount_amount: 500,        // 實際折抵金額
        original_amount: 5000,
        final_amount: 4500,
        message: "優惠代碼可用，可折抵 NT$ 500"
    }
}
```

#### 2. 取得所有優惠代碼（管理後台）
```
GET /api/admin/promo-codes
Response: {
    success: true,
    data: [...]
}
```

#### 3. 新增優惠代碼
```
POST /api/admin/promo-codes
Body: {
    code: "SUMMER2024",
    name: "夏日特惠",
    discount_type: "percent",
    discount_value: 10,
    ...
}
```

#### 4. 更新優惠代碼
```
PUT /api/admin/promo-codes/:id
```

#### 5. 刪除優惠代碼
```
DELETE /api/admin/promo-codes/:id
```

### 早鳥/晚鳥優惠 API

#### 1. 檢查早鳥/晚鳥優惠
```
GET /api/early-late-bird/check?checkInDate=2024-06-01&nights=2&roomType=標準雙人房&totalAmount=5000

Response: {
    success: true,
    data: {
        early_bird: {
            available: true,
            discount_type: "percent",
            discount_value: 5,
            discount_amount: 250,
            message: "提前 30 天預訂，可享 5% 折扣"
        },
        late_bird: {
            available: false,
            message: "不符合晚鳥優惠條件"
        }
    }
}
```

#### 2. 取得所有早鳥/晚鳥優惠設定（管理後台）
```
GET /api/admin/early-late-bird-settings
```

#### 3. 新增/更新/刪除早鳥/晚鳥優惠設定
```
POST /api/admin/early-late-bird-settings
PUT /api/admin/early-late-bird-settings/:id
DELETE /api/admin/early-late-bird-settings/:id
```

---

## 💻 前端實作建議

### 1. 優惠代碼輸入區塊

```html
<!-- 優惠代碼輸入區 -->
<div class="promo-code-section" style="margin: 20px 0; padding: 15px; background: #f8f9fa; border-radius: 8px;">
    <div style="display: flex; gap: 10px; align-items: flex-end;">
        <div style="flex: 1;">
            <label style="display: block; margin-bottom: 5px; font-weight: 600;">優惠代碼</label>
            <input 
                type="text" 
                id="promoCodeInput" 
                placeholder="輸入優惠代碼（選填）"
                style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;"
            >
        </div>
        <button 
            type="button" 
            id="applyPromoCodeBtn"
            onclick="applyPromoCode()"
            style="padding: 10px 20px; background: #667eea; color: white; border: none; border-radius: 4px; cursor: pointer;"
        >
            套用
        </button>
    </div>
    <div id="promoCodeMessage" style="margin-top: 10px; font-size: 14px;"></div>
    <div id="promoCodeDiscount" style="margin-top: 5px; color: #10b981; font-weight: 600; display: none;"></div>
</div>
```

### 2. 價格計算邏輯更新

```javascript
// 優惠相關變數
let appliedPromoCode = null;        // 已套用的優惠代碼
let appliedEarlyBird = null;       // 已套用的早鳥優惠
let appliedLateBird = null;         // 已套用的晚鳥優惠

// 計算價格（更新版）
async function calculatePrice() {
    // ... 原有計算邏輯 ...
    
    let totalAmount = roomTotal + addonsTotal;
    let discountAmount = 0;
    let discountDetails = [];
    
    // 1. 計算早鳥/晚鳥優惠
    const earlyLateBirdDiscount = await calculateEarlyLateBirdDiscount(
        checkInDate, 
        nights, 
        roomTypeName, 
        totalAmount
    );
    
    if (earlyLateBirdDiscount) {
        discountAmount += earlyLateBirdDiscount.discount_amount;
        discountDetails.push(earlyLateBirdDiscount);
    }
    
    // 2. 計算優惠代碼折扣
    if (appliedPromoCode) {
        const promoDiscount = calculatePromoCodeDiscount(
            appliedPromoCode, 
            totalAmount, 
            roomTypeName
        );
        
        if (promoDiscount) {
            discountAmount += promoDiscount.discount_amount;
            discountDetails.push(promoDiscount);
        }
    }
    
    // 3. 計算最終金額
    const finalTotalAmount = Math.max(0, totalAmount - discountAmount);
    
    // 4. 計算訂金或全額
    const paymentAmount = document.querySelector('input[name="paymentAmount"]:checked').value;
    const depositRate = depositPercentage / 100;
    const paymentType = paymentAmount === 'deposit' ? depositRate : 1;
    const finalAmount = finalTotalAmount * paymentType;
    
    // 5. 更新顯示
    updatePriceDisplayWithDiscounts(
        averagePricePerNight, 
        nights, 
        totalAmount, 
        discountAmount,
        discountDetails,
        paymentAmount, 
        finalAmount, 
        addonsTotal
    );
}

// 套用優惠代碼
async function applyPromoCode() {
    const code = document.getElementById('promoCodeInput').value.trim().toUpperCase();
    const messageDiv = document.getElementById('promoCodeMessage');
    const discountDiv = document.getElementById('promoCodeDiscount');
    
    if (!code) {
        messageDiv.innerHTML = '<span style="color: #dc2626;">請輸入優惠代碼</span>';
        return;
    }
    
    // 取得當前訂房資訊
    const checkInDate = document.getElementById('checkInDate').value;
    const selectedRoom = document.querySelector('input[name="roomType"]:checked');
    if (!selectedRoom || !checkInDate) {
        messageDiv.innerHTML = '<span style="color: #dc2626;">請先選擇房型和日期</span>';
        return;
    }
    
    const roomTypeName = selectedRoom.closest('.room-option').querySelector('.room-name').textContent.trim();
    const totalAmount = parseInt(document.getElementById('totalAmount').textContent.replace(/[^0-9]/g, '')) || 0;
    
    try {
        const response = await fetch('/api/promo-codes/validate', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({
                code: code,
                totalAmount: totalAmount,
                roomType: roomTypeName,
                checkInDate: checkInDate
            })
        });
        
        const result = await response.json();
        
        if (result.success) {
            appliedPromoCode = result.data;
            messageDiv.innerHTML = `<span style="color: #10b981;">✓ ${result.data.message}</span>`;
            discountDiv.innerHTML = `折抵金額：NT$ ${result.data.discount_amount.toLocaleString()}`;
            discountDiv.style.display = 'block';
            
            // 重新計算價格
            calculatePrice();
        } else {
            appliedPromoCode = null;
            messageDiv.innerHTML = `<span style="color: #dc2626;">${result.message || '優惠代碼無效'}</span>`;
            discountDiv.style.display = 'none';
        }
    } catch (error) {
        console.error('驗證優惠代碼錯誤:', error);
        messageDiv.innerHTML = '<span style="color: #dc2626;">驗證優惠代碼時發生錯誤</span>';
    }
}
```

---

## 📊 價格顯示更新

### 更新價格顯示函數

```javascript
function updatePriceDisplayWithDiscounts(
    pricePerNight, 
    nights, 
    totalAmount, 
    discountAmount,
    discountDetails,
    paymentType, 
    finalAmount, 
    addonsTotal
) {
    // 原有顯示邏輯...
    
    // 新增折扣顯示
    const totalAmountElement = document.getElementById('totalAmount');
    let html = '';
    
    // 房型總額
    const roomTotal = totalAmount - addonsTotal;
    if (addonsTotal > 0) {
        html += `<div style="margin-bottom: 5px; color: #666;">房型總額：NT$ ${roomTotal.toLocaleString()}</div>`;
        html += `<div style="margin-bottom: 5px; color: #666;">加購商品：NT$ ${addonsTotal.toLocaleString()}</div>`;
    }
    
    html += `<div style="margin-bottom: 5px; color: #333;">總金額：NT$ ${totalAmount.toLocaleString()}</div>`;
    
    // 顯示折扣明細
    if (discountAmount > 0) {
        discountDetails.forEach(discount => {
            html += `<div style="margin-bottom: 5px; color: #10b981;">${discount.name}：-NT$ ${discount.discount_amount.toLocaleString()}</div>`;
        });
        html += `<div style="border-top: 2px solid #ddd; padding-top: 5px; margin-top: 5px; font-weight: 700; font-size: 18px; color: #2C8EC4;">折抵後金額：NT$ ${(totalAmount - discountAmount).toLocaleString()}</div>`;
    }
    
    totalAmountElement.innerHTML = html;
    
    // 更新應付金額
    const paymentLabel = paymentType === 'deposit' ? `應付訂金 (${depositPercentage}%)` : '應付全額';
    document.getElementById('paymentTypeLabel').textContent = paymentLabel;
    document.getElementById('paymentAmount').textContent = `NT$ ${finalAmount.toLocaleString()}`;
}
```

---

## 🎯 實作優先順序建議

### 第一階段：基礎優惠代碼功能（1-2 週）
1. ✅ 建立 `promo_codes` 和 `promo_code_usages` 資料表
2. ✅ 實作優惠代碼 CRUD API
3. ✅ 實作優惠代碼驗證 API
4. ✅ 前端優惠代碼輸入區塊
5. ✅ 更新價格計算邏輯（整合優惠代碼）

### 第二階段：早鳥/晚鳥優惠（1 週）
1. ✅ 建立 `early_late_bird_settings` 資料表
2. ✅ 實作早鳥/晚鳥優惠 CRUD API
3. ✅ 實作早鳥/晚鳥優惠檢查 API
4. ✅ 更新價格計算邏輯（整合早鳥/晚鳥優惠）
5. ✅ 前端顯示早鳥/晚鳥優惠資訊

### 第三階段：進階功能（1 週）
1. ✅ 優惠組合規則（可疊加或互斥）
2. ✅ 使用次數限制檢查
3. ✅ 適用房型限制
4. ✅ 管理後台報表（優惠代碼使用統計）

---

## 💡 業務邏輯建議

### 優惠計算順序
1. **先計算早鳥/晚鳥優惠**（基於原始總金額）
2. **再計算優惠代碼折扣**（基於早鳥/晚鳥優惠後的金額，或原始金額，依設定而定）

### 優惠疊加規則
- **預設**：優惠代碼與早鳥/晚鳥優惠互斥（只能使用一種）
- **可選**：允許疊加（需在優惠代碼設定中啟用）

### 折扣上限
- 百分比折扣建議設定最高折抵金額上限
- 避免過度折扣影響營收

### 使用限制
- 每個優惠代碼可設定總使用次數限制
- 每個客戶可設定使用次數限制（防止濫用）
- 記錄每次使用，用於統計和限制檢查

---

## 📝 管理後台功能

### 優惠代碼管理
- 新增/編輯/刪除優惠代碼
- 查看使用記錄和統計
- 啟用/停用優惠代碼
- 匯出使用報表

### 早鳥/晚鳥優惠管理
- 新增/編輯/刪除優惠設定
- 設定優先級（多個優惠時選擇最優惠的）
- 查看適用情況統計

---

## ⚠️ 注意事項

1. **價格計算時機**
   - 在選擇日期、房型、加購商品時重新計算
   - 在套用優惠代碼時重新計算
   - 在切換付款方式時重新計算

2. **優惠驗證**
   - 每次訂房時重新驗證優惠代碼有效性
   - 檢查使用次數限制
   - 檢查有效期
   - 檢查最低消費金額

3. **資料一致性**
   - 訂房時記錄使用的優惠代碼和折扣金額
   - 取消訂房時不退回優惠代碼使用次數（或可選退回）

4. **安全性**
   - 優惠代碼驗證需在後端進行
   - 前端僅用於顯示，不可信任前端計算結果

---

## 🚀 後續擴展功能

1. **會員等級折扣整合**
   - 會員等級折扣可與優惠代碼疊加
   - 設定優先順序（例如：先套用會員折扣，再套用優惠代碼）

2. **促銷活動**
   - 限時優惠（特定日期範圍）
   - 滿額送（消費滿 X 元送 Y 元折扣）
   - 買多送多（住宿 N 晚送 1 晚）

3. **推薦獎勵**
   - 推薦碼系統
   - 推薦人和被推薦人都可獲得優惠

4. **積分系統**
   - 使用優惠代碼可獲得積分
   - 積分可兌換優惠代碼

---

**建議：先實作第一階段的基礎優惠代碼功能，驗證業務邏輯後再擴展到早鳥/晚鳥優惠。**





